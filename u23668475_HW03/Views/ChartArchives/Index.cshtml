@model IEnumerable<u23668475_HW03.Models.ChartArchive>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChartData)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChartData)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChartID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChartID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChartID })
        </td>
    </tr>
}

</table>
